<!--pages/my/index/index.wxml-->
<!-- 地区轮播图 -->
<swiper class="swiperBox" indicator-dots autoplay interval="2000" circular
	indicator-active-color="#ffffff" indicator-color="rgba(0, 0, 0, .3)"> 
    <swiper-item wx:for="{{imgUrls}}" wx:key="key" bindtap="imgClick" data-index="{{index}}">
        <image src="{{item}}"></image>
    </swiper-item>
</swiper>

<!-- <text></text>
<view class="labels_box">
    <view style="height: 50%;width: 100%;display: flex;flex-direction: row;" wx:for="{{2}}" wx:key="key">
        <view class="label_item_box" wx:for="{{4}}" wx:key="key" wx:for-index="index">
            <image src="/images/consult.png"></image>
            <text>客厅家具</text>
        </view>
    </view>
</view> -->
<!-- 地区图片 -->
<!-- <view class="areaBigBox">
    <view class="area-item" wx:for="{{imgUrls1}}" wx:key="key" wx:for-item="area" >
        <image 
            src="{{area.url}}" 
            class="area-image"
            hover-class="areaImgHover" 
            bindtap="imgClick" 
            data-index="{{index}}">
        </image>
        <view style="height: 50rpx;" class="mid">
            {{area.name}}
        </view>
    </view>
</view> -->
<view class="area-basic-box" wx:for="{{imgUrls1}}" wx:key="key" wx:for-item="area">
    <view class="area-basic-item-box">
        <image src="{{area.url1}}">
        </image>
        <view class="area-basic-item-text">
            <text class="mid">{{area.num1}}</text>
        </view>
    </view>
    <view class="area-basic-item-divline">
    </view>
    <view class="area-basic-item-box">
        <image src="{{area.url2}}">
        </image>
        <view class="area-basic-item-text">
            <text class="mid">{{area.num2}}</text>
        </view>
    </view>
</view>



<view class="store-basic-box" wx:for="{{imgUrls1}}" wx:key="key" wx:for-item="area">
    <view class="store-basic-item-box">
        <image src="{{area.url1}}">
        </image>
        <view class="store-basic-item-text">
            <text class="mid">{{area.num1}}</text>
        </view>
    </view>
    <view class="store-basic-item-divline">
    </view>
    <view class="store-basic-item-box">
        <image src="{{area.url2}}">
        </image>
        <view class="store-basic-item-text">
            <text class="mid">{{area.num2}}</text>
        </view>
    </view>
</view>







<view class="search_box mid" bindtap="clickToSearch">
    <text>搜索商家</text>
</view>
